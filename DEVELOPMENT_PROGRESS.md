# EAMS 开发进度

> 最后更新：2026-08-07
>
> 使用方式：每完成一个可验证的小步骤，就更新“已完成”和“下一步”。
> 完成的标准是代码可编译、可运行或可通过接口验证，而不是仅创建了文件。

## 当前状态

- 项目仓库、`backend` 与 `frontend` 目录已创建并提交到 GitHub。
- 项目根目录 `.gitignore` 已配置，忽略依赖目录、IDE 缓存、构建产物和本地配置。
- 后端已引入 Spring Boot、Web、Validation、Test 基础依赖。
- Spring Boot 已成功启动，服务端口为 `8080`。
- 后端阶段 1 基础设施已完成：统一响应、业务异常、全局异常处理。
- 前端已安装 Vue Router、Pinia、Axios、Element Plus。
- 前端统一 Axios 请求封装已通过 `npm run build` 验证。
- 前端入口已注册 Pinia 与 Element Plus。
- 阶段 2 已引入 MyBatis-Plus 与 MySQL 驱动，并创建本地数据源配置。
- 阶段 2 已引入 Spring Security 与 JJWT 依赖。
- 认证基础表已在本地 MySQL 执行成功，初始化脚本已保存为 `backend/sql/01_auth_tables.sql`，可用于交付复现。
- 阶段 2 正在实现真实数据库登录、JWT 校验与当前用户接口。
- 已修正 `DepartmentMapper` 的重复 `@Select` 注解冲突，认证模块后端代码已通过编译。
- 前端主布局、Dashboard 和业务页面占位路由已通过 `npm run build`；认证接入前需修正重复的路由定义。
- JWT 解析、认证过滤器、本地测试账号初始化、Pinia 认证状态和路由守卫代码已手写；尚待启动和接口联调验证。
- 当前前端构建通过，但登录页仍为占位内容，且当前用户类型字段需与后端响应字段对齐。
- 认证接口尚未完成真实登录、Token 校验和前端联调验证，阶段 2 不能标记为完成。

## 开发顺序

| 阶段 | 后端工作 | 前端工作 | 状态 |
| --- | --- | --- | --- |
| 1. 项目基础 | Spring Boot 启动、统一响应、异常处理 | Vue 基础依赖、目录、Axios 封装 | 已完成 |
| 2. 身份认证 | 登录、JWT、获取当前用户、权限控制 | 登录页、Token、Pinia、路由守卫 | 进行中 |
| 3. 基础数据 | 部门管理、管理员新增/禁用用户 | 主布局、部门管理、用户管理 | 未开始 |
| 4. 资产管理 | 资产增删改查、图片上传、报废 | 资产管理页、上传与预览 | 未开始 |
| 5. 申请领用 | 提交申请、审核、领用记录、归还 | 可申请资产、我的申请、审批、我的资产 | 未开始 |
| 6. 维修 | 提交、处理、完成维修 | 维修记录与处理页面 | 未开始 |
| 7. 收尾功能 | Dashboard、Redis 缓存、操作日志、Swagger | Dashboard 图表、操作日志 | 未开始 |
| 8. 交付 | SQL、测试、部署文档 | 构建、联调与演示 | 未开始 |

## 阶段 1：项目基础

### 已手写完成

- [x] 创建后端分层目录结构。
- [x] 在 `pom.xml` 引入 Spring Boot 父工程、Web、Validation、Test 依赖。
- [x] 创建 `EamsApplication` 并成功启动 Spring Boot。
- [x] 创建 `application.yml`。
- [x] 实现统一响应类 `common/Result` 与业务错误码枚举。
- [x] 安装 Vue Router、Pinia、Axios、Element Plus。
- [x] 创建前端 API 响应类型与 Axios 请求封装。
- [x] 执行 `npm run build`，验证 Axios 封装通过 TypeScript 构建。
- [x] 在 `main.ts` 注册 Pinia 与 Element Plus。

### 验收结果

- [x] 后端与前端基础代码已通过启动或构建验证。

## 阶段 2：身份认证

### 目标

- 基于 MySQL 用户、角色和状态实现真实登录。
- 使用 BCrypt 保存密码，使用 JWT 认证受保护接口。
- 提供 `POST /api/auth/login` 与 `GET /api/auth/me`。
- 前端完成登录页、用户状态管理和路由守卫。

### 下一步

1. 补齐登录表单，并修正前端当前用户字段与后端响应不一致的问题。
2. 启动后端，验证登录、当前用户、401、403 和禁用账号场景。
3. 完成前端登录、刷新恢复和退出登录联调后，标记阶段 2 完成。

## 本阶段规则

- 每进入一个新阶段或业务模块，先阅读对应需求、接口、数据库和开发任务文档。
- Controller 只负责接收请求、参数校验、调用 Service、返回统一响应；不写 SQL 和核心业务规则。
- 不新增仅用于演示、后续不会使用的临时代码。
- 所有完成项必须经过编译、启动或接口验证后才能勾选。
